document.getElementById(`${temple[i]}like`).innerHTML = localStorage.likesforMosesLake + "Likes";
function ${temple[i]}Count() {
        if (typeof(Storage) !== "undefined") {
            if (localStorage.`${temple[i]}likes`) {
                localStorage
            }           
        }
    }
